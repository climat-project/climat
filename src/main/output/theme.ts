import colors from 'colors';

export const error = colors.red;
export const warn = colors.yellow;
export const debug = colors.blue;
export const success = colors.green;
export const info = colors.cyan;
